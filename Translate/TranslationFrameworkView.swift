import SwiftUI

struct TranslationFrameworkView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Camera")
                .font(.largeTitle)
                .bold()

            Text("Camera translation UI will go here later.")
                .foregroundColor(.secondary)
                .multilineTextAlignment(.center)

            Spacer()
        }
        .padding()
    }
}
